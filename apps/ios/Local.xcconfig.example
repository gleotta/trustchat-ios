// Copy to Local.xcconfig (gitignored). Included by Debug.xcconfig and Release.xcconfig.
//
// TrustChat SMP queue-creation password (the PASS part of smp://<fingerprint>:<PASS>@host:port).
// Injected into Info.plist as TrustChatSMPPassword. Must not contain '@'. Leave unset if the server has no password.
TRUSTCHAT_SMP_PASSWORD =
